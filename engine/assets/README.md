# Runtime Assets

This directory contains source assets that are copied next to the built executable as `assets/`.

- `models/` is intended for mesh files loaded through Assimp.
- `textures/` is intended for image files loaded through `stb_image`.
